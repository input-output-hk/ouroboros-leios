This investigation took place in 2026 February.

- `RelationalStorage.md` is a higher-level characterization of what the relevant data is for the LeiosFetch logic.
  It has plenty of stubs, marked as TODO.

- `LeiosFetchLogic.md` is a baseline design for the LeiosFetch logic.
  It is extremely concrete, in order to surface feasbility challenges early on.
  It's not the only possible baseline design, but it sketches what a design focusing on minimal latency might look like.
  It has plenty of stubs, marked as TODO.
